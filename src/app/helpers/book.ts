export interface BookDetails {
  title: string;
  author: string;
  description: string;
  image: string;
}
export interface ReviewDetails {
  bookName: string;
  name: string;
  review: string;
  rating: number;
}
